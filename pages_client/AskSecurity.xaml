<Page x:Class="Provider.pages_client.AskSecurity"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Provider.pages_client"
      mc:Ignorable="d" 
      d:DesignHeight="253" d:DesignWidth="355"
      FontFamily="/resources/fonts/Nunito-ExtraBold.ttf#Nunito">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Grid.ColumnSpan="2" HorizontalAlignment="Center" FontSize="30" Foreground="{DynamicResource MaterialDesignBody}" Content="Безопасность"  />

        <TextBlock Grid.Row="1" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   FontSize="20" 
                   Foreground="{DynamicResource MaterialDesignBody}"
                   TextWrapping="Wrap" 
                   TextAlignment="Center"
                   Text="Добавьте дополнительный способ входа с помощью электронной почты и пароля" />

        <Button Grid.Row="3"
                Margin="5"
                Height="40"
                FontSize="20"
                materialDesign:ButtonAssist.CornerRadius="10"
                Style="{StaticResource MaterialDesignOutlinedButton}"
                Content="Добавить" Click="Button_Click" />

    </Grid>
</Page>
